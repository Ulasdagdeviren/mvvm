import 'package:flutter/material.dart';

class AppColors {
  static const Color blackColors = Color(0xff00000);
  static const Color whiteColors = Color(0xffffffff);
  static const Color buttonColor = Colors.green;
}
